import { ActionTree, Commit } from 'vuex'

import { IRootState } from '../../types'

import { IAiGameState } from './types'

export enum EN_AiGameActions {}

export const actions: ActionTree<IAiGameState, IRootState> = {}
