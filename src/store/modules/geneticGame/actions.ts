import { ActionTree, Commit } from 'vuex'

import { IRootState } from '../../types'

import { IGeneticGameState } from './types'

export enum EN_GeneticGameActions {}

export const actions: ActionTree<IGeneticGameState, IRootState> = {}
