import { ActionTree, Commit } from 'vuex'

import { IRootState } from '../../types'

import { IFinishScreenState } from './types'

export enum EN_FinishScreenActions {}

export const actions: ActionTree<IFinishScreenState, IRootState> = {}
